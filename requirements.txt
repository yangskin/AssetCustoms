# AssetCustoms runtime dependencies (UE Editor Python)
# Target: Content/Python/ (UE auto-adds this to sys.path)
# Run deploy.ps1 to install
Pillow>=10.0.0
PySide6-Essentials>=6.5.0
shiboken6>=6.5.0
psd-tools>=1.9.0
